/*

#pragma strict
public var timer : float = 3; //maximum danger zone time
public var counter : float = 0; //keeps track of of how long we've been in the danger zone
public var dist : float;

function Start () {
	
}

function Update () {
dist =  Vector3.Distance(targetObject.transform.position, this.transform.position);
 if (dist < 0.5) {
 
 }

}

*/